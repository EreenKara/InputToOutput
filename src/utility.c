#include "utility.h"
#include "stdio.h"
bool isNumber(char* text)
{
    int i ;
    if (sscanf(text, "%d", &i) == 1) return True;
    return False;
}
bool isValid(char* text){

    
    
    return False;
}